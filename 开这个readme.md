# 开这个readme —— 张氏族谱系统维护指南

无论你懂不懂代码，照着本指南都能完成日常维护。
后面的是给想深入了解的看的，简单更新只要看前面的即可。



1、项目地址在：https://github.com/zsjzol/zsjzol.github.io   再外套了一层Cloudflare Pages
2、只需要基本的github知识即可，如果初始github账号信息丢失，只需要克隆一份即可。
3、截止2026年8月23日，AI的能力已经非常的强，可以任何问题都可以问AI。

如何给族谱加一个人？

1. 登录github账号，找到zsjzol.github.io项目，找到 `zupu.txt`在线打开即可，这个文件记录了所有族谱的信息
，如果想增加信息，在末尾新增一行即可：

   比如：张小明 |20260823 |男 |张张 |里里 |- |深圳 | | | |

   严格按照顺序填入信息，“|“符号不会打，就复制，顺心信息 `zupu.txt`上面有。

2. 族谱图的构建，严格匹配填入的：父亲，母亲，配偶，千万不要填错。对于缺少的信息，用“-”补位（就是减号，键盘横排数字键0右边的。

3. 每个人都可以建立个人文件夹，用来放入更加详细的内容，包括照片、文档、视频。

3.1 个人文件夹的建立规则：文件夹名称是个人的出生日期加名字的组合。比如 `20260823张小明`，这里的出生日期、姓名一定要和`zupu.txt`里添加的信息一致，才能够关联。

3.2    个人文件夹内文件规则：可以放置常见图片，视频格式，文档格式只支持txt。命名要求是名字加编号，比如“张三01.txt“，“张三02.txt"、"张三01.jpg"、"张三01.MP4”，不同的扩展名都要重新从01开始命名，不然无法识别。

4.  如果出现页面混乱，认证检查加入的内容，特别是父亲、母亲、配偶，这三项关乎匹配核心规则。













------------------------------------------------------------------------------------

## 一、这是什么

这是一个**纯本地、静态、单文件**的电子族谱网页：

- 祖辈在最上面，一代一代向下分支，呈树状结构；
- 每个节点显示：头像、姓名、出生/死亡日期；
- 夫妻之间有婚姻连线，父子（母女）之间有竖向连线；
- **点击任意人物**，右侧弹出详情面板，展示：
  - 该人的全部字段信息（出生地、生平等）；
  - **图集**（照片）；
  - **文档**（txt 文字资料，点击阅读）；
  - **视频**（点击播放）。

所有数据都存放在本目录的普通文件里，**不依赖任何数据库、任何外网服务**。
把整个文件夹复制到任何电脑上，双击 `start.bat` 即可运行。

---

## 二、如何本地调试

0. github上的代码整体clone下来（https://github.com/zsjzol/zsjzol.github.io）
1. 双击运行 **`start.bat`**（Windows）。
2. 脚本会自动启动一个本地服务器，并自动打开浏览器进入族谱页面。
3. 浏览器地址栏显示：`http://localhost:8000/index.html`。

> **重要**：不要直接双击 `index.html` 用文件方式打开！
> 页面启动时要读取 `zupu.txt` 等数据，只有通过本地服务器（http:// 开头）才能读取成功。

**如果你电脑没有 Python**：先安装 Python 3（安装时勾选 "Add Python to PATH"），再运行 `start.bat`。
修改了任何数据后，**刷新浏览器页面**即可看到新内容（无需重启服务器）。

---

## 三、目录与文件说明

本目录下各文件/文件夹的用途：

| 名称 | 类型 | 作用 | 是否需要维护 |
|---|---|---|---|
| `start.bat` | 文件 | 一键启动本地服务器 | 一般不用动 |
| `index.html` | 文件 | 族谱网页本体（全部逻辑都在这一个文件里） | 懂代码才动 |
| `zupu.txt` | 文件 | **族人信息记录表（最重要！）** | 每次加人必改 |
| `维护信息.txt` | 文件 | 维护人联系方式、家族寄语 | 建议更新 |
| `需求文档.txt` | 文件 | 最初的需求说明（参考） | 不用动 |
| `开这个readme.md` | 文件 | 本维护指南 | 可以增补 |
| `默认男.jpg` / `默认女.jpg` | 文件 | 没照片时的默认头像 | 可替换 |
| `18000101太爷/` 等个人文件夹 | 文件夹 | 存放每个人的照片、文档、视频 | **加资料时新建** |

---

## 四、核心数据：`zupu.txt`（人怎么加）

### 4.1 格式

`zupu.txt` 是一个纯文本文件，**每一行代表一个人**，各信息之间用 **`|`**（竖线）分隔。

```
姓名 | 出生日期 | 性别 | 父亲 | 母亲 | 配偶 | 出生地 | 死亡日期 | 死亡地点 | 死亡原因 | 生平
```

- 以 `##` 开头的行是注释，不会被读取（但 **`##FIELDS:` 行是特殊定义行，见 4.2**）。
- 空行会被忽略。
- 某一格填写 `-` 表示"没有此信息"（等同留空，且**不会参与亲属匹配**）。

### 4.2 字段对照表（11 个字段）

文件里用一行 `##FIELDS:` 来声明各列的含义（按顺序对应）：

```
##FIELDS:姓名 |出生日期 |性别 |父亲 |母亲 |配偶 |出生地 |死亡日期 |死亡地点 |死亡原因 |生平
```

| 列号 | 字段 | 说明 | 示例 |
|---|---|---|---|
| 1 | 姓名 | 必填。**亲属关系完全靠它精确匹配** | 张朝玉 |
| 2 | 出生日期 | 农历为主。8 位数字（19330403）；只知年份写 4 位（1901）；完全不知写 0 或留空 | 19330403 |
| 3 | 性别 | 男 / 女（影响节点颜色和默认头像） | 女 |
| 4 | 父亲 | 填**父亲的姓名**（必须与父亲那行"姓名"完全一致） | 张黎民 |
| 5 | 母亲 | 填母亲的姓名 | 邓淑贞 |
| 6 | 配偶 | 填配偶的姓名 | 任泽凡 |
| 7 | 出生地 | 文字 | 荆门 |
| 8 | 死亡日期 | 同出生日期格式 | 20210123 |
| 9 | 死亡地点 | 文字 | 公安 |
| 10 | 死亡原因 | 文字 | 自然死亡 |
| 11 | 生平 | 生平简介，可写较长文字 | 花甲逢丙… |

> 除了"姓名"必填，其他都可以留空。

### 4.3 同一人允许重复记录（自动合并）

- 系统按 **姓名 + 出生日期** 认定"是不是同一个人"。
- 同一个人可以写多行，后面记录里**非空**的字段会覆盖前面的旧值。
- 用途：某人出生时信息不全，先登记一笔，后补资料时再补一行，不用改旧行。

### 4.4 亲属关系 = "姓名精确匹配"（最容易踩坑！）

系统**不识字、不做智能判断**，亲属关系纯粹靠名字**一字不差**地对上号：

- **父亲优先**：先拿该行"父亲"的名字去全表找同名的人，找到就挂到他名下；
- 找不到父亲，再拿"母亲"的名字找；
- **配偶匹配**：拿"配偶"名字找同名且**还没配对**的人，先到先得；
- 配偶中先出现的那一方是"主方"，子女统一挂到主方名下（不影响显示）。

**所以写名字时务必与原记录完全一致**，包括异体字、繁体字、错别字都不行。当前数据里就有几处"别名不一致"，是系统允许的挂靠写法（见 4.5 警告）。

### 4.5 当前数据中的"不一致姓名"（请知悉，别误改）

现有数据中有几处**孩子行写的父母名字与本人记录不完全一致**，系统因此把孩子挂到了"能匹配上的那一方"：

| 孩子 | 记录的父亲 | 记录的母亲 | 实际挂靠 |
|---|---|---|---|
| 张杰、任长跃、张兵、张世新、任长勤、张世俭 | `諱澤凡` | 张朝玉 | 挂母亲张朝玉（`諱澤凡`≠`任泽凡`） |
| 张天立 | `张培详` | 张世新 | 挂母亲张世新（`张培详`≠`张培祥`） |
| 任修颖 | 任长跃 | `李梅` | 挂父亲任长跃（`李梅`≠`李春梅`） |

> 如果想"修正"成双亲连线：把上面的名字改得和本人记录一字不差即可（如 `諱澤凡`→`任泽凡`）。
> 但改之前请想清楚——这是当年录入时保留的写法，改动会改变树形，改完务必刷新检查。

---

## 五、个人文件夹（照片/文档/视频放哪里）

### 5.1 文件夹命名：出生日期 + 姓名

每个人的个人文件夹，名字必须是 **`出生日期` + `姓名`**，且**和 `zupu.txt` 里那行记录完全一致**：

- zupu 行：`张朝玉 |19330403 |…` → 文件夹：`19330403张朝玉`
- zupu 行：`张运晟 |19920406 |…` → 文件夹：`19920406张运晟`
- zupu 行：`张开心 |20251229 |…` → 文件夹：`20251229张开心`

> 三个注意点：
> 1. 出生日期写的是 8 位（如 `19330403`），文件夹就用 8 位；
> 2. 出生日期写 `0` 的，文件夹就是 `0张庭凯`（页面会自动探测，不必纠结名字怪）；
> 3. 出生日期**留空**的，文件夹就是纯姓名（如 `张杰`）。
>
> **文件夹建了但 zupu.txt 里没有这个人 → 页面不显示**（例如目录里的 `18000101太爷/`、`18000102太奶/` 目前是测试残留，zupu 里没登记就不会出现在树上）。

### 5.2 文件夹内的文件命名：姓名 + 两位编号

规则：**`姓名` + `两位数字编号` + `扩展名`**，编号从 `01` 开始：

```
19330403张朝玉/
  ├─ 张朝玉01.jpg      ← 头像（编号最小的图片）
  ├─ 张朝玉02.jpg      ← 图集第 2 张
  ├─ 张朝玉01.txt      ← 文档（生平故事、访谈记录等）
  ├─ 张朝玉02.txt
  └─ 张朝玉01.mp4      ← 视频
```

要点：
- 编号必须是**两位数**：`01`、`02` … `99`，最多 99 个；
- 文件名里的"姓名"与 zupu 记录、文件夹前缀**三者一致**；
- 图片 / 文档 / 视频分别独立编号都从 `01` 开始（即可以同时有 `张朝玉01.jpg` 和 `张朝玉01.txt`）；
- **系统会自动连续探测**：从 01 开始找，同编号下会尝试 文档 → 图片 → 视频；
  如果某编号三种文件都没有，就计入"连续缺失"，**连续 2 个编号都没有文件，就停止探测**。
  因此**编号请连续使用**，不要跳号（比如放了 01、02、04，04 之前的 03 缺失若与 05 连成两个空号，04 以后就读不到了）。

### 5.3 支持的扩展名（格式）

| 类型 | 支持的扩展名 |
|---|---|
| 图片 | `.webp .jpg .jpeg .png .gif .bmp`（另含大写 `.JPG .JPEG .PNG .GIF .BMP .WEBP`） |
| 文档 | `.txt`（编码自动识别 UTF-8 / GBK，用记事本保存都行） |
| 视频 | `.mp4 .webm .mov .avi .mkv .ogg` |

> **图片大小写**：老照片扫描件常见 `xxx.JPG`（大写）。系统已同时探测大小写，一般都能读到。

### 5.4 头像规则

- 头像取个人文件夹里**编号最小的那张图**（即最先找到的 `xx01` 图片）；
- 没有图 → 按性别显示 `默认男.jpg` / `默认女.jpg`；
- 想换头像：把想当头像的图改成编号最小即可。

### 5.5 文件编码说明

- 图片、视频：正常放入即可，无需处理；
- txt 文档：支持 UTF-8 与 GBK/GB2312 编码。Windows 记事本默认 UTF-8 保存，直接能用。

---

## 六、`维护信息.txt`（维护人名片）

文件内容：
- 当前维护人姓名、电话；
- 最后维护日期；
- 家族寄语（记载了系统缘起：墓碑二维码、让后人扫码看族谱）。

**每次维护完，建议更新维护人和日期**，方便后人知道"上一任是谁"。

---

## 七、添加一名新成员：标准流程（照做即可）

场景：要给族谱加一个人（比如新出生的孩子、补录一位长辈）。

1. **打开 `zupu.txt`**（用记事本即可），在末尾**新增一行**：
   ```
   张小明 |20260823 |男 |张运驰 |周茜 |- |深圳 | | | |
   ```
   依次填写：姓名、出生日期、性别、父亲、母亲、配偶、出生地……（没有的写 `-` 或留空）。
   - 父亲/母亲名字**必须**和他们在 zupu 里的记录**一字不差**；
   - 如果这位新人和 zupu 里已有的某位是配偶关系，把双方的"配偶"栏都写上对方名字。
2. **保存 `zupu.txt`**。
3. （可选，强烈建议）新建个人文件夹 `20260823张小明/`，把照片、文档、视频按 **5.2** 的规则放进去。
4. **刷新浏览器页面**：
   - 确认新人在树上出现在正确的位置（挂在其父/母名下）；
   - 点击新人，确认右侧详情、图集、文档、视频都能显示。

给已有的人补资料：只需做第 3、4 步（甚至不用动 zupu.txt）。

---

## 八、常见问题与注意点（坑）

| # | 现象 | 原因 / 解决办法 |
|---|---|---|
| 1 | 双击 index.html 打开是空白/报错 | 必须用 `start.bat`（http 方式）打开 |
| 2 | 新加的人没出现在树上 | 父亲/母亲名字与谱中记录不一致，或挂在"性别无关"的字段名打错了；检查 4.4、4.5 |
| 3 | 两个人被系统当成了同一个人 | 姓名+出生日期完全相同就会合并。同名不同人的，出生日期必须区分开 |
| 4 | 头像没显示（显示默认图） | 文件夹名与 zupu 的"出生日期+姓名"不一致；或图片扩展名/命名不合规；或编号跳号 |
| 5 | 文档/视频读不到 | 命名必须是 `姓名+两位编号`；视频扩展名暂不支持大写；文档只支持 txt |
| 6 | 树连线上"只有一个家长" | 该行父亲或母亲名字在谱中匹配不上（见 4.5 的例子），系统按"单亲"规则布局 |
| 7 | 排序/层级不对 | 出生日期尽量准确；辈分靠父子关系字段，不靠日期 |
| 8 | 编号跳号导致后面的文件读不到 | 编号必须连续，连续 2 个空号就停止探测（见 5.2） |
| 9 | 想删掉某人 | 直接删掉 zupu.txt 里对应行，同时可删掉其个人文件夹 |
| 10 | 想改某人生平/照片 | 改 zupu.txt 对应行 / 更新文件夹内文件，刷新即可；同人多行时新行非空字段覆盖旧行 |
| 11 | 新增了文件夹页面没反应 | 确认该人在 zupu.txt 里已登记，且文件夹名与"出生日期+姓名"一致；刷新页面 |
| 12 | 中文/特殊字符文件名 | 系统已做 URL 编码处理，正常用中文名没问题；避免文件名里带 `#` `?` `%` 等特殊符号 |

---

## 九、给懂代码的人：系统技术说明（可选阅读）

- **单文件架构**：`index.html` 内包含全部 HTML/CSS/JS，无任何外部依赖、无 CDN，离线可用。
- **启动流程**（`init()`）：
  `bootStart()` 进度条 → 预读 `维护信息.txt` → `fetch('zupu.txt')` → `parseZupu()` 解析字段 → `mergePeople()` 按 姓名+出生日期 合并 → `buildTree()` 建立父子/夫妻关系 → `layoutTree()` 布局坐标 → `renderTree()` 渲染 → `probeAll()` 逐个探测个人文件夹 → `bootFinish()` → `centerTree()`。
- **关键函数**：
  - `parseZupu()`（~689 行）：解析，`##` 注释、`##FIELDS:` 字段定义、`-`→空；
  - `mergePeople()`（~721 行）：同一人合并；
  - `buildTree()`（~740 行）：父优先、母其次挂靠；配偶先到先得；子女并入 primary；
  - `calcSubtreeWidth()` / `layoutNode()`：子树宽度计算与坐标布局；
  - `appendNodeLinks()`：连线绘制（夫妻横线、子女竖线、单亲居中规则）；
  - `probePersonFiles()`（~1715 行）：文件夹探测（编号 01-99、`MISS_LIMIT=2`、`IMG_EXTS`/`VIDEO_EXTS`）；
  - `renderFilesSection()`：详情面板的文件区（图集/文档/视频）。
- **布局/连线规则**：
  - 夫妻：并排，中点向下连线；多子女时"中点竖线 + 横线 + 每子竖线"，单子女直接一条竖线；
  - 单亲（无配偶）：本人水平居中于子女群上方，"中心点向上竖线直连单亲，向下按子女规则分线"；
  - 单传链（每代一个孩子）逐层居中，避免偏置。
- **常量**（~655 行）：`NODE_W=150`、`NODE_H=96`、`H_SPACING=30`、`V_SPACING=60`、`MAX_INDEX=99`、`MISS_LIMIT=2` 等，想调间距/布局可改这里。

---

## 十、未来扩展建议（给后人）

- 墓碑二维码：把 `start.bat` 启动后的页面部署到任何静态服务器，生成二维码贴在墓碑上即可扫码查看；
- 需要更多字段（如"学历""职业"）：在 `##FIELDS:` 行末尾追加列名，同时给现有行补列即可，解析器支持扩展；
- 更多媒体类型：在 `index.html` 的 `IMG_EXTS` / `VIDEO_EXTS` 数组里追加扩展名。

---

*最后更新：2026 年 8 月 23 日。祝家族信息代代相传。*
